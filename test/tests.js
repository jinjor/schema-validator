const schemaValidator = require('../src/index.js');
const chai = require('chai');
const assert = chai.assert;

describe('schema-validator', function() {
  describe('#validate()', function() {
    it('should work', function() {
      assert.fail();
    });
  });
});
